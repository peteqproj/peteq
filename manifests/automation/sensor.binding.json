{
  "id": "http://json-schema.org/geo",
  "$schema": "http://json-schema.org/draft-06/schema#",
  "description": "Sensor binding aggregate",
  "type": "object",
  "properties": {
    "metadata": {
       "$ref": "../metadata.json#/properties/metadata" 
    },
    "spec": {
        "type": "object",
        "required": [
            "automation",
            "sensor"
        ],
        "properties": {
            "automation": {
                "type": "string"
            },
            "sensor": {
                "type": "string"
            }
        }
    }
  },
  "required": ["metadata", "spec"]
}